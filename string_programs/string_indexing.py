#string indexing


s='luminar'    #the elements are stored in variables as single element in each shell
               #also they are stored near to the previous one so they are called linear indexing
               # the positions of storing elements as 0 1 2 3.... and from the end ..,-2 -1
print(s[2])